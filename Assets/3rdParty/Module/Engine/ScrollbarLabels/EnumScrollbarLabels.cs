namespace Module.Engine.ScrollbarLabels {
    public enum BlockStateType { None, Active }
    public enum EyeStateType { None, Shown, Hidden }
    public enum LightStateType { Disable, Enable }
    public enum StateType { None, Active }
}
