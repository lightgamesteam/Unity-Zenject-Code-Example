namespace Module.Engine.ScrollbarLayers {
    public enum EyeStateType { None, Shown, Hidden }
    public enum LightStateType { Disable, Enable }
    public enum StateType { None, Active }
    
    public enum SelectStateType { None, Active }
    public enum ItemStateType { Disable, Enable }
    public enum SubContentType { None, Hide, Show }
}
